ДЗ_5. Хуки и формы.
Ссылка на ДЗ и описание:
https://github.com/BubbaCat/HW_5
Для наиболее внимательных в описании приятный подарок :)
═════════════════════════════════════════════════

Как делать?

    Реализуйте задание из репозитория выше. Пользуйте подсказками, если совсем тяжело
    Создайте новый репозиторий у себя в github, выполните задание в нём (Внимательно читайте как располагать файлы ДЗ) 
    Создайте пустую ветку master(или main)
    Создайте ветку HW_5/feature
    Реализуйте ДЗ.
    Откройте Pull Request HW_5/feature -> master(или main)
    Разместите ссылку на репозиторий в форму  ниже


═════════════════════════════════════════════════

ДЕДЛАЙН:
07.05.2026


freemorobskyr@gmail.com Сменить аккаунт
 
Не будет видно получателю
 
*Обязательный вопрос
Ссылка на ваш ТГ аккаунт
(что мы могли связать ТГ + Гитлаб и давать вам ОС)
*
Ссылка на ваш репозиторий
*
Как правильно использовать useContext для получения значения из контекста?
🔹 a) const value = useContext(MyContext.Provider)
🔹 b) const value = MyContext.useContext()
🔹 c) const value = useContext(MyContext)
🔹 d) const value = React.getContext(MyContext)

Когда следует использовать useCallback?
🔹 a) Для кэширования сложных вычислений
🔹 b) Для мемоизации функции, чтобы избежать лишних ререндеров дочерних компонентов
🔹 c) Для хранения DOM-элементов
🔹 d) ) Для создания кастомных хуков

Что должно быть обязательно сделано с дочерним компонентом, чтобы useCallback работал правильно?

Что делает useMemo
Запоминает компонент и предотвращает его ререндер
Кэширует результат вычислений и пересчитывает только при изменении зависимостей
Сохраняет предыдущее значение состояния
Создает мемоизированную функцию

Как правильно использовать React.memo


сonst MemoizedComponent = React.memo(Component, arePropsEqual?)
const MemoizedComponent = React.memo(() => Component);
const MemoizedComponent = Component.memo();
const MemoizedComponent = useMemo(Component);

Что выведется в консоль, если компонент с таким кодом дважды быстро ререндерится (например, из-за родителя)?
const Component = () => { const renderCount = useRef(0); useEffect(() => { renderCount.current += 1; console.log("Рендеров:", renderCount.current); }); return <div>Тест</div>; };

🔹 a) Рендеров: 1 (только один раз)
🔹 b) Рендеров: 1, затем Рендеров:
🔹 c) Рендеров: 2 (пропускает первый рендер)
🔹 d) Ничего (хук не сработает)

Почему useMemo в этом примере не предотвращает лишние вычисления, несмотря на мемоизацию? 
важно: items никак не кэшируется родителем выше!
const Component = ({ items }) => { const sortedItems = useMemo( () => items.sort((a, b) => a.id - b.id), [items] // items — массив объектов ); return <List items={sortedItems} />; }; Для лучшей читаемости вставьте код в редактор кода.


🔹 a) useMemo не работает с массивами
🔹 b) items передается как новый массив при каждом рендере, даже если элементы те же
🔹 c) Нужно использовать useCallback вместо useMemo
🔹 d) sort() мутирует исходный массив, и React видит изменения

Какую основную задачу решает useRef в React?


Хранить состояние компонента, которое не вызывает ререндер при изменении
Создавать мемоизированные вычисления для оптимизации производительности
Получать доступ к DOM-элементам или сохранять мутабельные значения между рендерами
Управлять глобальным состоянием приложения

В каких случаях лучше использовать useReducer, а не useState?


Когда нужно работать с примитивными значениями (числами, строками)
Когда состояние компонента простое и не требует сложной логики обновления
Когда состояние сложное и содержит несколько значений, или логика обновления зависит от предыдущего состояния
Когда нужно кэшировать результаты вычислений

Как работает React.memo? Зачем его нужно использовать? Когда бы вы его применяли? Как он сравнивает пропсы?


Лекция продлилась 2 часа. Комфортно ли было слушать и усваивать так долго материал?
от 1 до 5.
1 - хочется короче
5 - все ок.
1
 
2
 
3
 
4
 
5
 

Хватило ли лайвкодинга?
1
 
2
 
3
 
4
 
5
 

Как вам лекция?
1
 
2
 
3
 
4
 
5
 

Понравилось ли домашнее задание?
1
 
2
 
3
 
4
 
5
 

Насколько сложным оказалось домашнее задание?
1
 
2
 
3
 
4
 
5
 

Достаточно ли понятное было объяснение от лектора?
1
 
2
 
3
 
4
 
5
 

Достаточно ли информации в презентации? 
1
 
2
 
3
 
4
 
5
 
Общая обратная связь. Можете поделиться тут своими мыслями в случае необходимости)

Что хотелось бы, чтобы мы улучшили?
Критику мы воспринимаем крайне здраво, не переживайте!

Сделаю обязательным, чтобы вы чего-нибудь да написали :)


Никогда не используйте формы Google для передачи паролей.
Компания Google не имеет никакого отношения к этому контенту. - Связаться с владельцем формы - Условия использования - Политика конфиденциальности

Эта форма кажется вам подозрительной? Пожаловаться
Google Формы